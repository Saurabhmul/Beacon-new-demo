{
  "customer_guid": "string",
  "payment_history": "string (max 3 lines summarizing last 3 months of payments - include amounts, dates, and whether each was Received or Failed)",
  "conversation": "string (max 3 lines summarizing relevant conversations)",
  "vulnerability": "true or false",
  "reason_for_vulnerability": "string (max 5 lines, only if true)",
  "affordability": "MUST be exactly one of: HIGH, MEDIUM, LOW, VERY LOW, NOT SURE. Apply NMPC calculation: if NMPC=0 use VERY LOW not NOT SURE. NOT SURE only when zero data exists.",
  "reason_for_affordability": "MANDATORY - must not be empty. Show: 1) NMPC calculation from payment data, 2) MAD value, 3) NMPC/MAD percentage, 4) which threshold it matches. Example: 'NMPC=$0 (all payments failed). MAD=$251. 0/251=0% → VERY LOW'",
  "willingness": "MUST be exactly one of: HIGH, MEDIUM, LOW, VERY LOW, NOT SURE. Base on behavioral evidence. NOT SURE only when zero interaction data exists.",
  "reason_for_willingness": "MANDATORY - must not be empty. Cite specific evidence. Example: 'Customer proactively emailed requesting help on 12/11 → HIGH' or 'No response to 2 outreach attempts, broken PTP → LOW'",
  "ability_to_pay": "number or null",
  "reason_for_ability_to_pay": "string (max 5 lines)",
  "problem_description": "string (max 5 lines describing the customer's core problem)",
  "problem_confidence_score": "integer 1-10",
  "problem_evidence": "string (max 5 lines citing specific data)",
  "proposed_solution": "string (max 5 lines)",
  "solution_confidence_score": "integer 1-10",
  "solution_evidence": "string (max 5 lines citing which decision rule matched)",
  "internal_action": "string (max 5 lines)",
  "proposed_email_to_customer": "Plain text starting with 'Subject: ...' then 'Body: ...' OR 'NO_ACTION'. Do NOT wrap in JSON objects.",
  "arrears_clearance_plan": "object or null. Only populate when Clear Arrears Plan is the recommended treatment. Contains: monthly_payment_recommended (number), surplus_above_mad (number), total_arrears (number), months_to_clear (number), projected_timeline (array of {month: number, payment: number, remaining_arrears: number}). If recommended treatment is NOT Clear Arrears Plan, set to null."
}